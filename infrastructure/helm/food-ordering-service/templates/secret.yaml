apiVersion: v1
kind: Secret
metadata:
  name: {{ include "food-ordering-service.fullname" . }}
  labels:
    {{- include "food-ordering-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: {{ .Values.datasource.password | quote }}
