apiVersion: v1
kind: Service
metadata:
  name: {{ include "food-ordering-service.fullname" . }}
  labels:
    {{- include "food-ordering-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "food-ordering-service.selectorLabels" . | nindent 4 }}
